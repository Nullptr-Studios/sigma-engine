#include "Pawn.hpp"

namespace FNFE {

void Pawn::Init() { Actor::Init(); }
void Pawn::Start() { Actor::Start(); }
void Pawn::Update(double delta) { Actor::Update(delta); }
void Pawn::Destroy() { Actor::Destroy(); }

}
